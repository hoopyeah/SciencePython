'''
Created on Feb 11, 2015

@author: hoopyeah
'''
import sys
import math


def main():
    x_list = [300, 360, 490, 630, 700]
    y_list = [.3, .36, .49, .63, .7]
    for x in x_list:
        n_1 = 1 + (696.2 * x ** 2) / (x ** 2 - 68.4 ** 2) + (407.9 * x ** 2) / (x ** 2 - 116.2 ** 2) + (897.5 * x ** 2) / (x ** 2 - 9896.2 ** 2)
        n_2 = 1 + (696.2 * x ** 2) / (x ** 2 - 4678.56) + (407.9 * x ** 2) / (x ** 2 - 13502.44) + (897.5 * x ** 2) / (x ** 2 - 97934774.44)
        n_3 = 1 + x ** 2 * (696.2 / (x ** 2 - 4678.56) + 407.9 / (x ** 2 - 13502.44) + 897.5 / (x ** 2 - 97934774.44))
        print str(x) + ': ' + str(math.sqrt(n_1)) + ' , ' + str(math.sqrt(n_2)) + ' , ' + str(math.sqrt(n_3))
    for y in y_list:
        m_1 = 1 + (.6962 * y ** 2) / (y ** 2 - .0684 ** 2) + (.4079 * y ** 2) / (y ** 2 - .1162 ** 2) + (.8975 * y ** 2) / (y ** 2 - 9.8962 ** 2)
        print str(y) + ': ' + str(math.sqrt(m_1))
main()
